
 - web_terminal.mjs
   - move font atlas stuff to webworker_graphics.js
   - fix font packer
   - scale + blur
   - webgl2 renderer

 - code editor using web_terminal.mjs
   - color picker

 - figure out what to do with inlined source URLs in production builds: while I
   assume ES6-style imports will be resolved by a compiler like Babel I'm less
   sure about workers, wasm, etc. until a solution is found the non-import
   cases should be marked with
      // XXX:URLHARDCODED
   to make them easier to find.

 - trim acorn if only tokenizer is needed?

